import 'package:flutter/material.dart';

///Any re-usable Styles are referenced from this file.

class Styles {
  //todo: make backgroundImage have state and change depending on time of day
  static const backgroundImage = AssetImage("assets/mainPhotos/Night_Sky.jpg");
  static const boldTextStyles = TextStyle(fontWeight: FontWeight.bold);
}
